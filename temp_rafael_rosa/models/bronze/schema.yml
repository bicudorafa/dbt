version: 2

models:
  - name: stg_cnpjs
    description: "TBD"
    config:
      tags: ["intermediary"]
    columns:
      - name: buyer_tax_id
        description: 'TBD'
        tests:
          - not_null
          - unique
  - name: stg_installments
    description: "TBD"
    config:
      tags: ["intermediary"]
    columns:
      - name: invoice_id
        description: 'TBD'
        tests:
          - not_null
          - unique
